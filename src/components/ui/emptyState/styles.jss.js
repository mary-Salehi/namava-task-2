import { theme } from "../../../styles/theme";

export const emptyStateStyles = {
  emptyState: {
    width: "400px",
    textAlign: "center",
    fontSize: "14px",
    color: theme.colors.gray3,
    margin: '0 auto',
  },
};
